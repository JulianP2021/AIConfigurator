[project]
name = "aiconfigurator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "transformers>=5.12.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.17",
]
